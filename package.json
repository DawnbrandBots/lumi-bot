{
  "name": "lumi-bot",
  "version": "0.0.1",
  "repository": "https://github.com/DawnbrandBots/lumi-bot.git",
  "license": "AGPL-3.0-or-later",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "postclean": "tsc --build --clean tsconfig.json tsconfig.build.json",
    "start": "node --enable-source-maps --env-file-if-exists=.env dist",
    "lint": "eslint --flag unstable_native_nodejs_ts_config .",
    "lint:fix": "eslint --flag unstable_native_nodejs_ts_config --fix .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "register": "node --enable-source-maps --env-file-if-exists=.env scripts/registerCommands.ts",
    "bot:profile:update": "node --enable-source-maps --env-file-if-exists=.env scripts/updateBotProfile.ts",
    "db:recreate": "node --enable-source-maps --env-file-if-exists=.env scripts/recreateDb.ts",
    "test:db": "node --enable-source-maps --env-file-if-exists=.env scripts/recreateDb.ts ./test/mikro-orm.test.config.ts",
    "pretest": "yarn test:db",
    "test": "vitest"
  },
  "engines": {
    "node": ">=24",
    "yarn": "1.22.x"
  },
  "dependencies": {
    "@mikro-orm/core": "^7.1.6",
    "@mikro-orm/sqlite": "^7.1.6",
    "debug": "^4.4.3",
    "discord.js": "~14.27.0",
    "fuse.js": "^7.5.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@mikro-orm/cli": "^7.1.6",
    "@tsconfig/node24": "^24.0.4",
    "@types/debug": "^4.1.13",
    "@types/node": "^26.1.1",
    "eslint": "^10.7.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.7.0",
    "prettier": "3.9.5",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.64.0",
    "vitest": "^4.1.10"
  },
  "optionalDependencies": {
    "bufferutil": "^4.1.0",
    "zlib-sync": "^0.1.10"
  },
  "mikro-orm": {
    "preferTs": true,
    "configPaths": [
      "./src/mikro-orm.config.ts"
    ]
  }
}
